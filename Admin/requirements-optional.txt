# ========== 可选依赖 - OpenTelemetry ==========
# 仅在 BEACON_OTEL_ENABLED=1 时需要
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
opentelemetry-exporter-otlp-proto-http==1.43.0
opentelemetry-instrumentation==0.64b0
opentelemetry-instrumentation-django==0.64b0
opentelemetry-instrumentation-requests==0.64b0

# ========== 可选依赖 - LDAP/AD 登录 ==========
# 仅在启用 LDAP/AD 登录时需要：
# - 环境变量：BEACON_LDAP_ENABLED=1
# - 代码入口：Admin/app/utils/LdapAuth.py
ldap3==2.9.1

# ========== 可选依赖 - Cloud SaaS v1（S3 presign）==========
# 仅在 `BEACON_DEPLOYMENT_MODE=cloud` 且启用 presign API 时需要
boto3==1.43.47

# ========== 可选依赖 - 数字人防重放共享存储 ==========
# 配置 BEACON_DIGITAL_HUMAN_REPLAY_REDIS_URL 时需要；多实例部署建议启用
redis==8.0.1

# ========== 可选依赖 - 本地/Edge 使用 PostgreSQL ==========
# 配置 BEACON_CLOUD_DB_URL=postgresql://... 时需要；Cloud 镜像已内置
psycopg2-binary==2.9.12
