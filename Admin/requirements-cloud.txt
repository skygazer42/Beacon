# Beacon Cloud SaaS v1（云端模式）最小依赖
#
# 目的：
# - 给 docker-compose POC / Cloud 部署使用
# - 避免安装 opencv / numpy 等重依赖导致镜像构建失败或过慢
#
# 注意：
# - 这是 Cloud 运行时依赖（不含 pyinstaller 等打包工具）
# - Edge/本地交付仍可继续使用 `requirements-linux.txt` / `requirements-windows.txt`
# - Cloud 镜像实际安装 `requirements-cloud.lock`；修改本文件后必须按 lock 文件头部命令重新生成

# Web 框架
Django==6.1

# 系统监控（控制台首页会展示系统资源）
# psutil 7.x 提供受支持 Python 版本的 manylinux / musllinux 预编译 wheel，
# Cloud 镜像可以继续执行 binary-only 安装而不引入编译器。
psutil==7.2.2

# HTTP 请求（Edge/Cloud 内部 API 调用等）
requests==2.34.2

# Safe parsing for device-supplied ONVIF XML
defusedxml==0.7.1

# Production WSGI server and self-contained static file serving
gunicorn==26.1.0
whitenoise==6.12.0
waitress==3.0.2

# Image processing (used by Algorithm view for uploads/previews)
Pillow==12.3.0

# 授权签名校验（License Manager / Ed25519）
cryptography==50.0.0

# Cloud SaaS v1：S3 presign / 读取截图对象
boto3==1.43.78

# Postgres 驱动（BEACON_CLOUD_DB_URL=postgres://...）
psycopg2-binary==2.9.12
